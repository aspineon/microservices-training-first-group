server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/landrzejewski/microservices-training-first-group
          search-paths: configuration-files
